[build]
  command = "mkdir -p dist && cp index.html dist/index.html && cp style.css dist/style.css && npm --prefix vintageSalon install && npm --prefix vintageSalon run build && mkdir -p dist/newdd && cp -R newDD/* dist/newdd/"
  publish = "dist"
